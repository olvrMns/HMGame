import { Container } from "pixi.js";
import { Buildable } from "../util/Buildable";

export class Table extends Container implements Buildable {

    constructor() {
        super()
    }

    public build() {

    }
}